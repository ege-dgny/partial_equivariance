defaults:
  - base_pefm

env:
  env_class: push_t
  num_eef: 1
  args:
    num_eef: 1
    max_episode_length: 100

model:
  symmetry:
    group_type: so2
    distribution_type: projected_normal
    num_samples: 8
    entropy_weight: 0.01
